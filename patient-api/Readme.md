Patient Management REST API

Tech: Core PHP, MySQL, MVC Pattern

Endpoints:
GET    /api/patients
GET    /api/patients/{id}
POST   /api/patients
PUT    /api/patients/{id}
DELETE /api/patients/{id}

Run:
1. Import hospital_db
2. Place in htdocs
3. Test using Postman